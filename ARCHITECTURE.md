# Portfolio CMS - System Architecture

## 🏗️ Architecture Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                         CLIENT BROWSER                          │
├─────────────────────────────────────────────────────────────────┤
│  Public Site                    │          Admin Panel          │
│  - Dynamic Pages                │          - Dashboard          │
│  - Component Rendering          │          - Management UIs     │
│  - Contact Forms                │          - Configuration      │
└──────────────┬──────────────────┴───────────────┬───────────────┘
               │                                  │
               │ HTTP Requests                    │ HTTP Requests
               │                                  │
┌──────────────▼──────────────────────────────────▼───────────────┐
│                       EXPRESS SERVER                            │
├─────────────────────────────────────────────────────────────────┤
│                        MIDDLEWARE LAYER                         │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐         │
│  │   Auth       │  │  Site Config │  │    Errors    │         │
│  │  Middleware  │  │   Middleware │  │   Handler    │         │
│  └──────────────┘  └──────────────┘  └──────────────┘         │
├─────────────────────────────────────────────────────────────────┤
│                         ROUTES LAYER                            │
│  ┌────────────────────────────┐  ┌───────────────────────────┐ │
│  │      Public Routes         │  │      Admin Routes         │ │
│  │  - GET /                   │  │  - GET /admin/login       │ │
│  │  - GET /:slug              │  │  - GET /admin/dashboard   │ │
│  │  - POST /contact           │  │  - GET /admin/pages       │ │
│  └────────────────────────────┘  │  - POST /admin/api/*      │ │
│                                   └───────────────────────────┘ │
├─────────────────────────────────────────────────────────────────┤
│                      CONTROLLERS LAYER                          │
│  ┌────────────────────────────┐  ┌───────────────────────────┐ │
│  │   Public Controller        │  │    Admin Controller       │ │
│  │  - renderPage()            │  │  - showDashboard()        │ │
│  │  - submitContactForm()     │  │  - showPages()            │ │
│  │  - viewProject()           │  │  - createPage()           │ │
│  │  - viewArticle()           │  │  - updateConfig()         │ │
│  └────────────────────────────┘  └───────────────────────────┘ │
├─────────────────────────────────────────────────────────────────┤
│                       DATABASE LAYER                            │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │              Database Configuration Module                │  │
│  │  - executeQuery()                                         │  │
│  │  - executeTransaction()                                   │  │
│  │  - safeJsonParse()                                        │  │
│  └──────────────────────────────────────────────────────────┘  │
└──────────────────────────┬──────────────────────────────────────┘
                           │
                           │ SQL Queries (libSQL Protocol)
                           │
┌──────────────────────────▼──────────────────────────────────────┐
│                      TURSO DATABASE                             │
│                    (Serverless SQLite)                          │
├─────────────────────────────────────────────────────────────────┤
│  Tables:                                                        │
│  - site_config          - components                            │
│  - users               - component_types                        │
│  - pages               - text_content                           │
│  - sections            - projects                               │
│  - articles            - navigation_items                       │
└─────────────────────────────────────────────────────────────────┘
```

## 🔄 Request Flow

### Public Site Request Flow

```
1. User visits http://example.com/projects
   ↓
2. Express router matches GET /:slug
   ↓
3. loadSiteConfig middleware loads config and navigation
   ↓
4. publicController.renderPage() is called
   ↓
5. Controller queries database:
   - Get page by slug
   - Get sections for page
   - Get components for each section
   - Get text content keys
   - Get projects/articles if needed
   ↓
6. Data passed to page.ejs template
   ↓
7. EJS renders HTML with components
   ↓
8. HTML + CSS + JS sent to browser
   ↓
9. User sees fully rendered page
```

### Admin Request Flow

```
1. User visits http://example.com/admin/components
   ↓
2. Express router matches GET /admin/components
   ↓
3. isAuthenticated middleware checks session
   ↓
4. If not authenticated → redirect to login
   If authenticated → continue
   ↓
5. adminController.showComponents() is called
   ↓
6. Controller queries database:
   - Get all components with joins
   - Get all sections
   - Get all component types
   ↓
7. Data passed to admin/components.ejs
   ↓
8. Admin UI rendered with forms and tables
   ↓
9. User interacts with UI
   ↓
10. AJAX request to PUT /admin/api/components/:id
    ↓
11. Controller validates and updates database
    ↓
12. JSON response sent back
    ↓
13. UI updated with success/error message
```

## 📊 Database Entity Relationships

```
┌──────────────┐
│    pages     │
│──────────────│
│ id           │◄────┐
│ slug         │     │
│ title        │     │
│ is_published │     │
└──────────────┘     │
                     │
              ┌──────┴──────┐
              │             │
        ┌─────▼──────┐      │
        │  sections  │      │
        │────────────│      │
        │ id         │◄─────┤
        │ page_id    │      │
        │ section_key│      │
        └─────┬──────┘      │
              │             │
        ┌─────▼──────────┐  │
        │   components   │  │
        │────────────────│  │
        │ id             │  │
        │ section_id     │  │
        │ component_type │  │
        │ config (JSON)  │  │
        │ styles (JSON)  │  │
        └────────────────┘  │
                            │
┌──────────────┐            │
│ text_content │            │
│──────────────│            │
│ content_key  │            │
│ content_value│            │
└──────────────┘            │
                            │
┌──────────────┐            │
│ site_config  │            │
│──────────────│            │
│ config_key   │            │
│ config_value │            │
└──────────────┘            │
                            │
┌──────────────┐            │
│   projects   │────────────┘
│──────────────│
│ id           │
│ title        │
│ slug         │
└──────────────┘

┌──────────────┐
│   articles   │
│──────────────│
│ id           │
│ title        │
│ slug         │
└──────────────┘

┌──────────────┐
│    users     │
│──────────────│
│ id           │
│ username     │
│ password_hash│
└──────────────┘
```

## 🎨 Component Rendering Process

```
┌─────────────────────────────────────────────┐
│         Component Configuration             │
│─────────────────────────────────────────────│
│ {                                           │
│   "text_key": "home_hero_title",            │
│   "subtitle_key": "home_hero_subtitle"      │
│ }                                           │
└──────────────────┬──────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────┐
│         Load Text Content                   │
│─────────────────────────────────────────────│
│ text_content["home_hero_title"]             │
│ → "Hi, I'm a Full-Stack Developer"          │
└──────────────────┬──────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────┐
│         Component Template (EJS)            │
│─────────────────────────────────────────────│
│ <div class="hero">                          │
│   <h1><%= textContent[config.text_key] %></h1>│
│   <p><%= textContent[config.subtitle_key] %>│
│ </div>                                      │
└──────────────────┬──────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────┐
│         Rendered HTML                       │
│─────────────────────────────────────────────│
│ <div class="hero">                          │
│   <h1>Hi, I'm a Full-Stack Developer</h1>   │
│   <p>I build beautiful applications</p>     │
│ </div>                                      │
└─────────────────────────────────────────────┘
```

## 🔐 Authentication Flow

```
┌─────────────────────────────────────────────┐
│  User visits /admin/dashboard               │
└──────────────────┬──────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────┐
│  isAuthenticated middleware checks session  │
└──────────────────┬──────────────────────────┘
                   │
        ┌──────────┴──────────┐
        │                     │
        ▼                     ▼
┌────────────┐        ┌────────────────┐
│ Has userId │        │ No userId      │
│ in session?│        │ in session     │
└─────┬──────┘        └────────┬───────┘
      │                        │
      │ YES                    │ NO
      │                        │
      ▼                        ▼
┌────────────┐        ┌────────────────┐
│ Continue   │        │ Redirect to    │
│ to route   │        │ /admin/login   │
└────────────┘        └────────────────┘
```

### Login Process

```
1. User enters username/password
   ↓
2. POST to /admin/login
   ↓
3. Query database for user
   ↓
4. Compare password with bcrypt
   ↓
5. If valid:
   - Create session
   - Set userId in session
   - Redirect to dashboard
   ↓
6. If invalid:
   - Show error message
   - Stay on login page
```

## 📦 Module Dependencies

```
server.js
├── express
├── express-session
├── dotenv
├── routes/
│   ├── public.js
│   │   └── controllers/publicController.js
│   │       └── config/database.js
│   │           └── @libsql/client
│   └── admin.js
│       └── controllers/adminController.js
│           ├── config/database.js
│           └── bcrypt
├── middleware/
│   ├── auth.js
│   ├── siteConfig.js
│   │   └── config/database.js
│   └── errors.js
└── views/ (EJS templates)
```

## 🔧 Configuration Cascade

```
Environment Variables (.env)
        ↓
Site Config Table (database)
        ↓
CSS Variables (injected at runtime)
        ↓
Component Styles (JSON → inline styles)
```

Example:
```
.env:
TURSO_DATABASE_URL=...
        ↓
Database:
primary_color = "#2563eb"
        ↓
Template:
<style>
:root {
  --primary-color: #2563eb;
}
</style>
        ↓
Component:
<button style="background: var(--primary-color)">
```

## 🚀 Deployment Architecture

```
┌─────────────────────────────────────────────┐
│              CDN / Edge Network             │
│         (Optional - Cloudflare)             │
└──────────────────┬──────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────┐
│           Node.js Application               │
│        (Render / Railway / Vercel)          │
├─────────────────────────────────────────────┤
│  - Express Server                           │
│  - EJS Rendering                            │
│  - Session Management                       │
│  - Static File Serving                      │
└──────────────────┬──────────────────────────┘
                   │
                   │ libSQL Protocol
                   │ (over HTTPS)
                   ▼
┌─────────────────────────────────────────────┐
│              Turso Database                 │
│           (turso.tech servers)              │
├─────────────────────────────────────────────┤
│  - Distributed SQLite                       │
│  - Edge Replicas                            │
│  - Automatic Backups                        │
└─────────────────────────────────────────────┘
```

## 💾 Data Flow Patterns

### Read Pattern (GET request)
```
Browser → Express → Middleware → Controller → Database Query
                                                    ↓
Browser ← HTML/JSON ← Template/JSON ← Data ← Query Result
```

### Write Pattern (POST/PUT request)
```
Browser → Express → Auth Check → Validation → Controller
                                                    ↓
Browser ← JSON Response ← Success/Error ← Database Update
```

### Configuration Update Pattern
```
Admin Panel → AJAX POST → Validate → Update Database
                                          ↓
                            Cache Invalidation (next request)
                                          ↓
                            Public Site Reflects Changes
```

## 🎯 Key Design Patterns Used

1. **MVC Architecture**
   - Models: Database queries
   - Views: EJS templates
   - Controllers: Request handlers

2. **Middleware Pattern**
   - Authentication
   - Configuration loading
   - Error handling

3. **Template Method Pattern**
   - Dynamic component rendering
   - Layout inheritance

4. **Strategy Pattern**
   - Multiple component types
   - Configurable behavior

5. **Repository Pattern**
   - Database abstraction
   - Query utilities

## 🔄 State Management

### Server State
- **Sessions**: User authentication state
- **Database**: All content and configuration
- **In-Memory**: None (stateless server)

### Client State
- **No Framework**: Pure JavaScript
- **Form State**: Managed via DOM
- **UI State**: CSS classes and attributes

## 📈 Scalability Considerations

### Current Scale
- Designed for: 1-1000 concurrent users
- Database: Turso (scales automatically)
- Server: Single instance (can scale horizontally)

### Scale-Up Path
1. Add Redis for session storage
2. Add CDN for static assets
3. Deploy multiple server instances
4. Use Turso's read replicas
5. Add caching layer (Redis/Memcached)

## 🛡️ Security Layers

```
┌─────────────────────────────────────────────┐
│         HTTPS (Transport Security)          │
└──────────────────┬──────────────────────────┘
                   │
┌──────────────────▼──────────────────────────┐
│       Session Authentication               │
└──────────────────┬──────────────────────────┘
                   │
┌──────────────────▼──────────────────────────┐
│       Route Protection (Middleware)         │
└──────────────────┬──────────────────────────┘
                   │
┌──────────────────▼──────────────────────────┐
│       Input Validation                      │
└──────────────────┬──────────────────────────┘
                   │
┌──────────────────▼──────────────────────────┐
│       Parameterized Queries                 │
└──────────────────┬──────────────────────────┘
                   │
┌──────────────────▼──────────────────────────┐
│       Database (Turso with Auth Token)      │
└─────────────────────────────────────────────┘
```

## 🎓 Summary

This architecture provides:
- ✅ **Separation of Concerns**: Clear layers and responsibilities
- ✅ **Scalability**: Can grow with your needs
- ✅ **Maintainability**: Easy to understand and modify
- ✅ **Security**: Multiple layers of protection
- ✅ **Flexibility**: Database-driven configuration
- ✅ **Performance**: Efficient queries and caching
- ✅ **Extensibility**: Easy to add new features

The system is production-ready and can handle real-world traffic while remaining simple enough to understand and customize.
